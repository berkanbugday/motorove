/**
 * User Stats Interface
 * Represents user statistics for posts, events, followers, and following
 */
export interface IUserStats {
  postsCount: number;
  eventsCount: number;
  followersCount: number;
  followingCount: number;
}
