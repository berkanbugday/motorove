/**
 * Node environment types
 */
export enum NodeEnv {
  Dev = 'development',
  Staging = 'staging',
  Prod = 'production',
}

export enum NodeEnvFileName {
  development = '.env.dev',
  staging = '.env.staging',
  production = '.env.prod',
}
